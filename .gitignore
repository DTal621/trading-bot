# secrets — never commit
.env
*.pem
*.key

# python
__pycache__/
*.pyc
.venv/

# generated report outputs are committed by Actions into reports/out/,
# but local test runs shouldn't be — Actions manages that path in CI.
