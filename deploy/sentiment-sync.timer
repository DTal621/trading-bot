# Runs the sync every 30 minutes. Enable with:
#   sudo systemctl enable --now sentiment-sync.timer
[Unit]
Description=Sync sentiment bot state every 30 min

[Timer]
OnCalendar=*:0/30
Persistent=true

[Install]
WantedBy=timers.target
