pyyaml>=6.0
anthropic>=0.40        # daily LLM review (API-billed, separate from your Claude plan)
alpaca-py>=0.30        # trading + historical news/bars (wire broker.py / news.py)
requests>=2.31         # Telegram Bot API calls
